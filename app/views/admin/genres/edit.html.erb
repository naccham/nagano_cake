<h1>ジャンル編集</h1>

<%= form_with model: @genre, url: admin_genre_path(@genre.id), local:true do |f| %>
  <lavel for="inputName">ジャンル名</lavel>
  <%= f.text_field :name, autofocus: true, id:"inputName", placeholder:"ジャンル名" %>
  <%= f.submit "変更を保存" %>
<% end %>
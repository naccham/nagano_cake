<h1>商品詳細画面</h1>

<div>
  <%= attachment_image_tag @item, :image, size: "200x200" %>
  <p>商品名：<%= @item.name %></p>
  <p>商品説明：<%= @item.introduction %></p>
  <p>税抜価格：<%= @item.price %>円</p>
  <%= link_to edit_admin_item_path(@item.id) do %>
      <p>編集する</p>
  <% end %>
</div>
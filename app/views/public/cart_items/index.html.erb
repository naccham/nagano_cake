<% @cart_items.each do |cart_item| %>
  <div>
    <td>商品名：<%= cart_item.item.name %></td>  
    <td>単価<%= (cart_item.item.price * 1.1).floor %></td>
    <td><%= cart_item.amount %>個</td>
    <td>小計<%= (cart_item.item.price * 1.1).floor * cart_item.amount %></td>
  </div>
<% end %>
